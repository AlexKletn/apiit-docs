import type {SignaturesItem} from "@/components/TypeDoc/api-docs.types.ts";

interface SignaturesTableProps {
  signatures: SignaturesItem[]
  withTitle?: boolean
}

export type {
  SignaturesTableProps
}
